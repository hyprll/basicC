/*
    &&	Logical AND
    ||	Logical OR
    !	Logical Not
*/
#include <stdbool.h>
#include <stdio.h>

int main(){
   
int age = 16;
  double height = 6.3;
 
  bool value = (age >= 18) || (height > 6.0);
  
  printf("%d ", value);


// coba aja sendiri gw laper!
return 0;
}